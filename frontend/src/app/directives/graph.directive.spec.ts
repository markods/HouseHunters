import { GraphDirective } from './graph.directive';

describe('GraphDirective', () => {
  it('should create an instance', () => {
    // const directive = new GraphDirective();   // FIXME
    // expect(directive).toBeTruthy();
  });
});
